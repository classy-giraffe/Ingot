# Ingot

## Agent skills

### Issue tracker

Issues and specs live in GitHub issues on `classy-giraffe/Ingot`, operated via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, label strings identical to role names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and ADRs at the repo root. See `docs/agents/domain.md`.
