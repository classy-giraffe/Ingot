# Ingot active slot A: /usr-only payload, whole-image erofs (zstd).
# The /usr subtree of the image root is copied in (CopyFiles=/usr:/).
# PARTUUID is embedded in the UKI's kernel command line (usr=).
# SplitName=slot: the disk output step extracts this partition as the
# standalone slot artifact <disk>.slot.raw - the unit of reproducibility
# and the sysupdate transfer.
[Partition]
Type=usr
UUID=0066bfe5-47f1-52dc-9a16-1bb10191a1dc
Label=ingot_0.1.0
Format=erofs
Compression=zstd
CopyFiles=/usr:/
SizeMinBytes=8G
SizeMaxBytes=8G
SplitName=slot
