# Ingot: filesystem modules for the runtime root (state partition and
# the active slot) and the live ISO media (spec 10.3). Loaded early in
# the initramfs by the stock systemd-modules-load service.
btrfs
erofs
iso9660
